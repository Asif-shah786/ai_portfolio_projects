name: ai_portfolio_projects
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # state management
  flutter_bloc: ^8.1.3

  # navigation
  go_router: ^14.6.2

  # ui
  shimmer: ^3.0.0

  get_it: ^8.0.3
  injectable: ^2.3.2
  injectable_generator: ^2.4.1
  flutter_hooks: ^0.20.3
  dio: ^5.7.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.14
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
